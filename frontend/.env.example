# Leave blank for same-origin deployments or the Vite dev proxy.
# For a separate backend, use the backend origin, for example:
# VITE_API_BASE=https://api.example.com
VITE_API_BASE=https://artisanbridgebackend.vercel.app
